{
  "date": "2026-09-26",
  "change": "Publish the skill as a Claude Code and Codex plugin; the Claude Code plugin supplies the simplifier profile and Codex keeps a derived repository profile.",
  "evaluation_type": "policy responses only",
  "method": "One fresh Claude Code subagent per scenario and snapshot, rubric withheld, told to read only the frozen skill snapshot.",
  "baseline_commit": "1d9b38d8f651810143794bdec283c8a7e588d1c1",
  "baseline_layout": "SKILL.md, references/, and agents/ at the repository root",
  "candidate_layout": "skills/research-repo-standard/ (SKILL.md, references/), agents/, and .claude-plugin/",
  "runs": [
    {
      "scenario": "AA",
      "snapshot": "baseline",
      "score": "2/4",
      "criteria": [
        "Resolved research-repo-standard:research-repo-standard and recorded the marketplace source.",
        "FAIL: wrote .claude/agents/research-code-simplifier.md as a verbatim copy and treated the plugin agent as not the repository profile.",
        "Wrote no Codex profile and no target AGENTS.md, CLAUDE.md, or CODEX.md.",
        "FAIL: the smoke test resolves the repository copy, not research-repo-standard:research-code-simplifier."
      ],
      "response": "I read SKILL.md, references/prerequisites.md, references/bootstrap.md, the Destruction, Simplifier review and Review waivers sections of references/governance.md, and agents/research-code-simplifier.md. I created, edited and ran nothing. Here is what the agent in the scenario does.\n\n## 1. Names I resolve and how\n\n**The skill: `research-repo-standard`.** Claude Code lists it as `research-repo-standard:research-repo-standard`, which means plugin `research-repo-standard` and skill `research-repo-standard`. The skill part matches the required name exactly, but seeing it in the list is not enough. I also confirm:\n- **Source:** Claude Code's plugin manager (`/plugin` and its marketplace view) shows the plugin, the marketplace it came from, and that marketplace's source. The source must be github.com/lucascamillomd/research-repo-standard.\n- **Details:** I note the version or commit, the install scope and the install path, if the host reports them.\n- **No duplicates:** no second `research-repo-standard` exists anywhere else, for example a personal skill installed with `npx skills add`. If there is one, I report the conflict and do not choose between them.\n- **README match:** the source matches the expected source and provenance in the project README (bootstrap's README checklist). If the README has no entry, it matches the source the user approved.\n- **Invocation:** loading this skill in the session is the functional check.\n\nIf the name or its source does not match, I stop. I report the exact name, the host, the resolver I tried and the result, and I ask before any reinstall or recovery.\n\n**The reviewer: `research-code-simplifier`.** The `research-repo-standard:research-code-simplifier` entry in the agent list is the plugin's own agent. It is not the repository profile. It does not count as installation and it cannot pass the smoke test. I do not disable or remove it, because that would change global agent settings without authorization. After I install the profile, the name I use is the plain `research-code-simplifier`, and it must resolve to the file in the repository.\n\n## 2. Provenance I record\n\nI record this once, at first use, and put it in the completion report. I create no separate tracking file and no lab-notebook entry for it.\n- **Host:** Claude Code, with the version it reports.\n- **Resolvers:** the skill list, the `/plugin` manager with its marketplace view, and the `/agents` list.\n- **Resolved source:** plugin `research-repo-standard` from the marketplace sourced at github.com/lucascamillomd/research-repo-standard, with the version or commit, scope and install path the host reports.\n- **Profile source:** the canonical `agents/research-code-simplifier.md` inside that verified install path. It is never taken from a path guessed from the current directory, and never from a local clone of the skill's source.\n- **Invocation evidence:** that the skill was invoked in this session, and the reviewer's own report from step 5.\n\nI reuse this record while the session and source stay the same. If the host, session or source changes, I report again.\n\n## 3. Checks before writing\n\nI look for leftovers from an earlier integration:\n- an `AGENTS.md`, `CLAUDE.md` or `CODEX.md` that restates the standard;\n- symlinks or wrapper files that point to a simplifier profile;\n- simplifier profiles in other places, such as a top-level `agents/` folder, a `code-simplifier` profile, or `.codex/agents/research-code-simplifier.toml`.\n\nFor each one I report its path, what it points to, and whether it was customized, and I leave it unchanged. Removing one would need the destruction procedure and explicit authorization. If `.claude/agents/research-code-simplifier.md` already exists with customized content, I do not overwrite it without explicit authorization. For a new scaffold I expect none of these, and I report exactly what I checked.\n\n## 4. Files I write\n\nOnly one: `<target-repo>/.claude/agents/research-code-simplifier.md`. It is an exact copy of the canonical profile from the verified plugin source. I confirm the copy is identical to the source (diff or checksum).\n\nI do not:\n- write `.codex/agents/*`;\n- create or modify `AGENTS.md`, `CLAUDE.md` or `CODEX.md`;\n- add host names to the profile or restate it in any other policy file;\n- copy SKILL.md or `references/` into the repository;\n- add plugin or marketplace entries to `.claude/settings.json`.\n\nThe README is part of the approved scaffold, not a host-integration file. If its `research-repo-standard` entry (exact name, expected source and provenance, recovery steps linked to prerequisites.md) is missing, I report that as a scaffold gap under the bootstrap checklist. I then check `git diff` and `git status`: the new profile should be the only change, with unrelated work left alone.\n\n## 5. What I verify: a real Claude Code smoke test\n\nWriting the file proves nothing about resolution. In a fresh Claude Code session in the target repository (restarting if the host has not picked up the new file), I check three things:\n1. `research-repo-standard` resolves by exact name, and its reported provenance matches the record from step 2.\n2. `/agents` resolves `research-code-simplifier` as a project agent at `<target-repo>/.claude/agents/research-code-simplifier.md`. It must be separate from, and not confused with, the plugin's `research-repo-standard:research-code-simplifier`.\n3. I launch `research-code-simplifier` through Claude Code's delegation with the repository instructions and a smoke-test-only scope: resolve and invoke `research-repo-standard` by exact name, then report the skill's provenance, your profile path and invocation evidence, with no review, edits or implementation. The reviewer reports its own provenance; I do not assume it from mine. It must match the expected source, and `git status` must show no new changes afterwards.\n\nIf the host, resolver or agent launch is unavailable or fails, I report that check as a manual verification boundary with the actual failed attempt, run the authorized recovery procedure, and do not call host integration complete. A waiver never counts as a passed smoke test.\n\n## Final message to the user (as the scenario agent)\n\n\"Host integration for Claude Code:\n- **Skill:** `research-repo-standard` resolved as `research-repo-standard:research-repo-standard`. The plugin comes from the marketplace sourced at github.com/lucascamillomd/research-repo-standard (version/commit <as reported>, scope <as reported>). It matches the README's expected source and is invoked in this session.\n- **Checks before writing:** I found no earlier-integration files: <list checked; or each found path, what it points to, customized yes/no, left unchanged>.\n- **File written:** `.claude/agents/research-code-simplifier.md` only, an exact copy of the canonical profile from the verified plugin source (identical checksum). No `AGENTS.md`, `CLAUDE.md` or `CODEX.md` was touched and no Codex profile was written.\n- **Plugin's own agent:** `research-repo-standard:research-code-simplifier` remains as the plugin supplies it. It is not the repository profile, and reviews launch `research-code-simplifier` from the repository file.\n- **Smoke test:** (1) skill resolved with matching provenance; (2) `research-code-simplifier` resolved at `<target-repo>/.claude/agents/research-code-simplifier.md`; (3) the reviewer, launched without an implementation request, reported that it resolved and invoked `research-repo-standard` from the expected source <its reported evidence>. `git status` shows only the new profile.\n\nOr, if any check could not run: '<check> could not run: <host/resolver/result>. This is a manual verification boundary, and host integration is not complete until it passes.'\""
    },
    {
      "scenario": "AA",
      "snapshot": "candidate",
      "score": "4/4",
      "criteria": [
        "Resolved research-repo-standard:research-repo-standard and recorded marketplace source and installed commit.",
        "Used research-repo-standard:research-code-simplifier and wrote no repository copy.",
        "Wrote no Codex profile and no target AGENTS.md, CLAUDE.md, or CODEX.md.",
        "Smoke test uses both plugin names; unavailable checks stay manual boundaries."
      ],
      "response": "As the scenario agent, I read `SKILL.md`, `references/prerequisites.md` (the owner of host integration), `references/bootstrap.md` (the \"Selected host integration\" section and the README checklist), the \"Destruction\" section of `references/governance.md`, and the plugin's `agents/research-code-simplifier.md`. The skill routes \"Capability first use, installation, changed provenance, or host integration\" to `prerequisites.md`, and I followed its \"Host profile installation\" and \"Selected-host smoke test\" procedures.\n\n## 1. Names I resolve\nI resolve names by exact name through Claude Code's own resolver, never by looking for a file on disk:\n- **Skill:** `research-repo-standard:research-repo-standard`, from the host's skill list.\n- **Simplifier profile:** `research-repo-standard:research-code-simplifier`, from the host's agent list.\n\nSeeing a name in a list is not enough on its own. Provenance must also verify, and the skill and agent must actually work when invoked (step 5).\n\nI do not resolve `superpowers:*`, `scientific-critical-thinking` or `nature-figure` here. Each is resolved just before the work that needs it, not as a blanket check.\n\n## 2. Provenance I record\nThe skill and agent lists show only names. So I get the source from Claude Code's plugin manager:\n- **Marketplace:** `research-repo-standard`, with source `github.com/lucascamillomd/research-repo-standard`.\n- **Plugin:** `research-repo-standard@research-repo-standard`.\n- **Installed commit:** the commit the plugin manager reports.\n\nI compare this with the plugin provenance the project README records under the bootstrap README checklist. If the README has no entry yet, I compare it with the source the user approved.\n\nThis first-use record goes in my report, not a repository file. It contains:\n- the host (Claude Code and its version);\n- the resolver (the native skill list, agent list and plugin manager);\n- the marketplace source, installed commit and plugin root path the host reports;\n- the canonical profile path, `<reported plugin root>/agents/research-code-simplifier.md`, taken from the verified install and never from the current directory or a source checkout;\n- the invocation evidence.\n\nI reuse this record while the session, host and provenance stay the same. I report again if any of them changes or resolution fails.\n\n## 3. Read-only check for an earlier integration\nThis check comes before the profile step, even though Claude Code needs no repository file. I look for three kinds of artifact:\n- **Legacy policy:** a target `AGENTS.md`, `CLAUDE.md` or `CODEX.md` that restates the standard.\n- **Alias:** a symlink or wrapper file that resolves to a simplifier profile.\n- **Generic simplifier:** a simplifier profile outside the host's expected path. Examples are `.claude/agents/research-code-simplifier.md`, a top-level `agents/` profile, a `code-simplifier` profile, or a stray `.codex/agents/research-code-simplifier.toml`.\n\nFor each one I report its path, the path it resolves to, and whether its content was customized. I leave it unchanged. Removing any of them needs explicit, narrow authorization under the governance \"Destruction\" section.\n\n## 4. Files I write in the repository\n**None.** On Claude Code the plugin itself supplies the profile as `research-repo-standard:research-code-simplifier`, so there is no repository copy:\n- no `.claude/agents/…` file;\n- no `.codex/agents/…toml`;\n- no `AGENTS.md`, `CLAUDE.md` or `CODEX.md` created or modified;\n- no global agent configuration changed.\n\nI do not edit the canonical profile, which stays host-neutral. If the scaffold's README lacks the plugin entry, I report that as a gap in the approved scaffold's README checklist rather than as a host-integration file.\n\n## 5. What I verify: the real smoke test in Claude Code\n1. `research-repo-standard:research-repo-standard` resolves by its exact name, and its reported provenance (marketplace source and installed commit) matches the README record or the approved source.\n2. `research-repo-standard:research-code-simplifier` resolves through the host, and I report its installed profile path at the verified plugin root.\n3. I launch that agent through Claude Code's delegation mechanism, passing the repository instructions and this scope. I tell it this is a host-integration smoke test: report resolution only, with no review, edits or implementation. It must resolve and invoke `research-repo-standard` by exact name and report, in its own words, the provenance, profile path and invocation evidence. I do not infer its resolution from my own environment.\n4. Afterwards, `git status` and `git diff` show that host integration changed nothing in the repository, and every earlier-integration artifact is untouched.\n\n**If something fails or is unavailable:**\n- I report the exact name, the host, the resolver I tried, and the result.\n- I install or recover nothing without authorization. The authorized route in Claude Code is `/plugin marketplace add lucascamillomd/research-repo-standard`, then `/plugin install research-repo-standard@research-repo-standard`, then a restart if the host requires it. After that I rediscover, re-verify the name and provenance, and resume from the blocked step.\n- If the host or resolver is unavailable, that is a manual verification boundary. It is never a simulated pass, and I don't call host integration complete.\n\n## Final message to the user\n> Claude Code host integration is complete.\n>\n> - **Skill:** `research-repo-standard:research-repo-standard` resolved in Claude Code's skill list and was invoked in this session. Its provenance is marketplace `research-repo-standard` (github.com/lucascamillomd/research-repo-standard), installed commit `<sha>`, which matches the README's recorded source.\n> - **Simplifier:** `research-repo-standard:research-code-simplifier` resolved in the agent list. Its profile is at `<plugin root>/agents/research-code-simplifier.md`.\n> - **Smoke test:** the delegated simplifier independently reported resolving and invoking `research-repo-standard` from that same source and commit. It made no edits.\n> - **Repository files written:** none. The plugin supplies the profile, so there is no `.claude/agents` copy and no `AGENTS.md`, `CLAUDE.md` or `CODEX.md`. `git status` shows no changes from this step.\n> - **Earlier-integration artifacts:** `<none found | list: path → resolves to, customized yes/no; left unchanged>`.\n> - **Remaining boundaries:** `<none | the exact check that could not run>`.\n>\n> Questions:\n> 1. If earlier-integration artifacts were listed, do you want any removed? Name the exact paths.\n> 2. Should the README keep the installed commit `<sha>` as the expected provenance, so that a plugin update counts as changed provenance to report and re-verify?\n\nIf any smoke-test step could not run, the message instead names the failed check, the resolver I tried and its result. It then asks for authorization to recover, and does not say integration is complete."
    },
    {
      "scenario": "AB",
      "snapshot": "baseline",
      "score": "4/4",
      "criteria": [
        "Resolved research-repo-standard:research-repo-standard and recorded the marketplace source.",
        "Derived .codex/agents/research-code-simplifier.toml from the profile in the installed plugin with the same name, description, and body.",
        "Wrote no Claude Code profile and no target AGENTS.md, CLAUDE.md, or CODEX.md.",
        "Smoke test through Codex; unavailable checks stay manual boundaries."
      ],
      "response": "I read SKILL.md, references/prerequisites.md, references/bootstrap.md and agents/research-code-simplifier.md from the snapshot. The snapshot's `references/prerequisites.md` owns this procedure. Here is how the scenario agent completes Codex host integration.\n\n## 1. Names I resolve (and names I leave alone)\n\n**`research-repo-standard`**\n- I resolve it by exact name through Codex's own skill listing, not by looking at files.\n- Codex shows it as `research-repo-standard:research-repo-standard`. That is the plugin name plus the skill name, the same form as `superpowers:brainstorming`. The skill part matches exactly, so I accept it and record the qualified form word for word.\n- If a second, unqualified `research-repo-standard` also resolves, for example a user-level skill install, I report the ambiguity. I don't pick one quietly.\n\n**`research-code-simplifier`**\n- I resolve it through Codex's custom-agent discovery, and only after writing the profile.\n- \"No custom agent exists yet\" is the expected state before that step. It is not a failure.\n\n**Names I leave alone:** `superpowers:*`, `scientific-critical-thinking` and `nature-figure`. The skill resolves each capability just before the work that needs it. Host integration doesn't need them, so there is no blanket preflight.\n\n## 2. Provenance I record\n\nI record these at first use, from what the host reports:\n- **Host:** Codex (app or CLI, with the version it reports).\n- **Resolver:** Codex's skill listing plus its Plugins view (`/plugins`).\n- **Listed name:** `research-repo-standard:research-repo-standard`.\n- **Resolved source:**\n  - plugin `research-repo-standard`, from the marketplace at github.com/lucascamillomd/research-repo-standard;\n  - the installed version as the host reports it (the cache folder name is `local`, not a release or commit);\n  - the marketplace revision, if Codex shows one;\n  - the SKILL.md path the resolver reports under `~/.codex/plugins/cache/research-repo-standard/research-repo-standard/local/`.\n- **Invocation evidence:** this session's successful invocation of the skill through Codex.\n- **Profile source:** the path of the canonical profile I derived the TOML from.\n\nI compare this with the expected source in the project README (the README checklist requires that entry).\n- If they differ, or Codex can't link the plugin to the approved GitHub source, provenance has not resolved. The `local` folder name alone proves nothing, and a file on disk is not resolution evidence.\n- In that case I report the exact name, host, resolver tried and result, and I ask before any reinstall or recovery. I don't edit the README to match whatever is installed.\n\nThe record goes in my integration and completion report. I create no separate tracking file, don't write it into `docs/LAB_NOTEBOOK.md`, and change no global Codex configuration.\n\n## 3. Files I write in the repository\n\n**Before writing, I look for earlier integrations and leave anything I find unchanged:**\n- a target `AGENTS.md`, `CLAUDE.md` or `CODEX.md` that restates the standard;\n- symlinks or wrapper files that point to a simplifier profile;\n- simplifier profiles in the wrong place, such as a top-level `agents/` profile, a `code-simplifier`, or a `.claude/agents/` profile.\n\nFor each one I report its path, where it resolves to, and whether its content was customized. Removing one is a separate step under the destruction procedure. I also check whether `.codex/agents/research-code-simplifier.toml` already exists, and I never overwrite a customized one without explicit authorization.\n\n**I write exactly one file:** `<target-repo>/.codex/agents/research-code-simplifier.toml`.\n- I write it myself; no script generates it.\n- I derive it from `agents/research-code-simplifier.md` next to the SKILL.md the resolver reported inside the plugin cache.\n- I don't use the current directory, a separate clone, or a fresh download from GitHub.\n\n```toml\nname = \"research-code-simplifier\"\ndescription = \"Independently simplify a completed research-code change while preserving supported use and scientific results. Use when delegated under research-repo-standard.\"\ndeveloper_instructions = '''\n<body after the closing frontmatter ---, unchanged>'''\n```\n\n- The file has only these three keys: no model, sandbox or other fields, and no host names added.\n- A literal (`'''`) string keeps the backslashes in the Python examples exactly as written. I checked that the body contains no `'''` and no trailing whitespace.\n- TOML drops the first newline after the opening `'''`, so I place the body so the parsed value equals it byte for byte.\n\n**What I don't write:**\n- no `.claude/agents/` profile;\n- no `AGENTS.md`, `CLAUDE.md` or `CODEX.md`, and no changes to existing ones;\n- no copy of SKILL.md, `references/` or the canonical profile;\n- no symlink or wrapper;\n- no project or global Codex config changes;\n- no README change.\n\n## 4. What I verify\n\n**The file:**\n- It parses as TOML with exactly three keys.\n- `name` and `description` match the canonical frontmatter; the description is the folded single line.\n- `developer_instructions` equals the canonical body exactly.\n- The canonical profile is unchanged.\n\n**The repository:**\n- The pre-commit hooks pass on the new file (TOML syntax, trailing whitespace, final newline), and `git diff --check` is clean.\n- `git status` shows only the new file, and `git check-ignore` confirms `.codex/` is not ignored, so the file can be committed.\n\n**The real smoke test in Codex:** I start a new Codex session in the repository, since custom agents may only load at session start or once the project is trusted. Then:\n1. `research-repo-standard` resolves by exact name with the same provenance as above.\n2. Codex resolves `research-code-simplifier` and reports the installed path `<target-repo>/.codex/agents/research-code-simplifier.toml`, not a user-level agent with the same name.\n3. I launch that agent with a smoke-test-only brief: no implementation and no edits. It must report, from its own environment:\n   - the profile path it loaded from;\n   - that it resolved and invoked `research-repo-standard` (its listed name and provenance), matching the expected plugin and marketplace source.\n\n   I don't infer its result from my own environment. `git status` must be unchanged afterwards.\n\n**If something fails:** if Codex, its skill listing, or subagent launch is unavailable, or the delegate can't resolve the skill, I report that as a manual verification boundary with the exact failure. I don't claim host integration is complete, and I run the recovery procedure only with authorization.\n\n## Final message (as the scenario agent)\n\n\"Codex host integration: I resolved `research-repo-standard` by exact name through Codex. It is listed as `research-repo-standard:research-repo-standard`, from plugin `research-repo-standard` in the marketplace at github.com/lucascamillomd/research-repo-standard. The cached version is `local`, at `~/.codex/plugins/cache/research-repo-standard/research-repo-standard/local/`. This matches the source recorded in the README. [Any earlier-integration artifacts: path, where it resolves, customized or not, left unchanged; or: none found.]\n\nI wrote one file, `.codex/agents/research-code-simplifier.toml`, from the canonical profile in that verified plugin source. It has the same name and description, and the body unchanged as `developer_instructions`. I did not create or change `AGENTS.md`, `CLAUDE.md` or `CODEX.md`, did not add a Claude profile, and made no global configuration changes. The TOML parses and matches the canonical profile exactly, pre-commit passes, `git diff --check` is clean, and git status shows only the new file.\n\nSmoke test in a fresh Codex session: Codex resolved `research-code-simplifier` from `.codex/agents/research-code-simplifier.toml`. The launched reviewer reported, from its own environment, that it resolved and invoked `research-repo-standard` from the same plugin source, and it made no edits.\n\nNote: the installed version shows as `local` rather than a pinned release or commit. [If the smoke test couldn't run: it is recorded as a manual verification boundary, and host integration is not reported complete.]\""
    },
    {
      "scenario": "AB",
      "snapshot": "candidate",
      "score": "4/4",
      "criteria": [
        "Resolved research-repo-standard:research-repo-standard and recorded marketplace source and installed commit.",
        "Derived .codex/agents/research-code-simplifier.toml from agents/ at the resolved plugin root with the same name, description, and body.",
        "Wrote no Claude Code profile and no target AGENTS.md, CLAUDE.md, or CODEX.md.",
        "Smoke test through Codex; unavailable checks stay manual boundaries."
      ],
      "response": "**How I would complete Codex host integration under research-repo-standard**\n\nThis is based on SKILL.md, `references/prerequisites.md` (it owns host integration), `references/bootstrap.md` (the README checklist and the selected-host step), `references/governance.md` (destruction and waivers) and the plugin's `agents/research-code-simplifier.md`. The repository is hypothetical, so I created, edited and ran nothing. Where a result only a real Codex run can give is needed, I leave a bracketed slot instead of making one up.\n\n---\n\n### 0. Scope\n\nHost integration is the step after the approved core scaffold, which the approved design and plan already authorize. It needs no new gate or second confirmation. I start by checking git status so I keep unrelated work intact.\n\nI do not resolve `superpowers:*`, `scientific-critical-thinking` or `nature-figure` now. Each one is resolved right before the work that depends on it, not as a blanket preflight.\n\n### 1. Names I resolve (through Codex's own skill and plugin listing, not the filesystem)\n\n1. **`research-repo-standard:research-repo-standard`**, by that exact name. The skill list showing it is a start, but I also need Codex to report its source:\n   - marketplace `research-repo-standard` at `github.com/lucascamillomd/research-repo-standard`\n   - plugin `research-repo-standard`\n   - the installed commit\n\n   Invoking the skill successfully in this session is the functional check.\n   - The cache path `~/.codex/plugins/cache/research-repo-standard/research-repo-standard/local/` is not resolution evidence. `local` is a cache or version label, not a commit, and a file on disk never proves resolution.\n   - If Codex does not report the commit, I report that gap. I do not infer the commit from the path.\n2. **The plugin root, which holds the canonical profile.** This is the root of the plugin whose source I just verified, as the resolver reported it: `<resolved plugin root>/agents/research-code-simplifier.md`, which should be the `.../local/agents/research-code-simplifier.md` above. It is never a path taken from the current directory, a source checkout or the target repo.\n3. **`research-code-simplifier`**, the Codex custom agent. No such agent exists yet, so I resolve it only after writing the profile, during the smoke test (section 5).\n\n### 2. Provenance I check and record\n\n- **Check:** the source Codex reports (marketplace source plus installed commit) must match what the scaffold README records for the plugin prerequisite, as the bootstrap README checklist requires. If the README has no entry, it must match the source the user approved.\n- **On a mismatch or failed resolution:** I stop the integration. I report the exact name, the host (Codex), the resolver I tried and what it returned. Then I ask for authorization before any reinstall or recovery: `codex plugin marketplace add lucascamillomd/research-repo-standard`, then install `research-repo-standard` from Plugins. I never change global Codex configuration without authorization.\n- **Record at first use, in the session and completion report:**\n  - host: Codex, plus version and session\n  - resolver: Codex native skill and plugin listing\n  - resolved source: the marketplace URL and installed commit\n  - resolved plugin root and canonical profile path\n  - invocation evidence\n- **Reuse:** I reuse this record while the session, host and source are unchanged. I re-report if any of them changes or resolution fails.\n- **Project history:** this is not a scientific decision, so it gets no `docs/LAB_NOTEBOOK.md` entry. The README stays as written if its entry already matches.\n\n### 3. Before writing: look for leftovers from an earlier integration\n\nI report each one found with its path, what it resolves to, and whether it was customized. I leave all of them unchanged.\n\n- **Legacy policy:** a target `AGENTS.md`, `CLAUDE.md` or `CODEX.md` that restates the standard.\n- **Alias:** a symlink or wrapper file that resolves to a simplifier profile.\n- **Generic simplifier:** a simplifier profile outside Codex's expected path. Examples: a top-level `agents/research-code-simplifier.md`, a `code-simplifier` profile, or `.claude/agents/research-code-simplifier.md`.\n- **Existing target file:** I also confirm `.codex/agents/research-code-simplifier.toml` is absent. If it exists and was customized, I do not overwrite it without explicit authorization.\n- **Removal:** only after this report and only with explicit authorization for the named targets, following the destruction procedure in `references/governance.md`.\n\n### 4. Files I write in the repository: exactly one\n\nCodex plugins cannot supply agents, so I write `<target-repo>/.codex/agents/research-code-simplifier.toml` myself. I write it directly with no copy script and no symlink. It carries the canonical profile's name, description and body:\n\n```toml\nname = \"research-code-simplifier\"\ndescription = \"Independently simplify a completed research-code change while preserving supported use and scientific results. Use when delegated under research-repo-standard.\"\ndeveloper_instructions = '''\n# Research code simplifier\n...body after the frontmatter, unchanged, through the final quote() example...\n'''\n```\n\n- The description is the frontmatter's multi-line value joined into one line.\n- The body goes into a TOML literal multi-line string (`'''`), so its backslashes (`'\\\"'\\\"'` in the `quote` example) are kept as written, not treated as escapes. I checked that the body contains no `'''`, so the delimiter is safe. TOML drops the newline right after the opening `'''`.\n- I do not write:\n  - a Claude Code profile, since Codex is the selected host;\n  - anything in `~/.codex/agents/` or other global configuration;\n  - any target `AGENTS.md`, `CLAUDE.md` or `CODEX.md`;\n  - copies of SKILL.md or `references/`.\n- I do not edit the canonical profile in the plugin cache or add host names to it.\n- The README would change only if it lacked the plugin-prerequisite entry the approved scaffold requires. I would report that gap and fill it with the verified source.\n\n### 5. What I verify\n\n**File-level checks:**\n- The TOML parses.\n- The parsed `name`, `description` and `developer_instructions` equal the canonical frontmatter name, the joined description and the unchanged body. I extract these from the verified plugin root.\n- `git status` and `git diff` show only the new `.codex/agents/research-code-simplifier.toml`.\n- No policy file was created or changed, and unrelated work is intact.\n- I run the repo's pre-commit or format check on the new file.\n\n**Smoke test in real Codex.** Writing the file proves nothing about resolution. I restart the session if Codex needs it to pick up the new custom agent. The test must show three things:\n1. Codex resolves `research-repo-standard:research-repo-standard` by exact name and reports provenance matching the README.\n2. Codex resolves the custom agent `research-code-simplifier` and reports its installed path, `<target-repo>/.codex/agents/research-code-simplifier.toml`.\n3. I launch that agent as a delegate with the repo instructions and a no-implementation scope. It goes just far enough to report its own evidence: that it resolved and invoked `research-repo-standard:research-repo-standard` from the expected marketplace source and commit, plus the profile path it is running from. I do not infer the delegate's resolution from my own environment.\n\n**If a check cannot run:** if Codex, its resolver or agent launch is unavailable, that is a manual verification boundary, not a simulated pass. I report the exact failed check, run the authorized recovery, and do not call the integration complete. Only after a real failed attempt may the user waive or defer the simplifier pass, and even then the smoke test is never reported as passed.\n\n---\n\n### Final message to the user\n\nCodex host integration for the research-repo-standard simplifier:\n\n- **Skill resolution:** Codex resolved `research-repo-standard:research-repo-standard` from marketplace `research-repo-standard` (`github.com/lucascamillomd/research-repo-standard`) at commit `[commit reported by Codex]`. That matches the source recorded in the README. The skill was invoked in this session. The cache folder name `local` was not used as provenance.\n- **Canonical profile:** `[resolved plugin root]/agents/research-code-simplifier.md`, taken from the plugin root the resolver reported, not from the working directory.\n- **Earlier integration artifacts:** `[none found | list with path, what it resolves to, and customized yes/no]`. I left all of them unchanged. Removing any of them needs your explicit authorization for the named paths.\n- **File written (one):** `.codex/agents/research-code-simplifier.toml`, a Codex custom agent with the canonical name, description and body, unchanged. It parses and matches the canonical profile exactly.\n- **Not written:** no Claude Code profile; no `AGENTS.md`, `CLAUDE.md` or `CODEX.md`; no global Codex configuration; the canonical profile is unchanged. `git status` shows only the new file.\n- **Smoke test in Codex:** `[pass | manual boundary with the exact failed check]`\n  1. Skill resolved by exact name, with matching provenance.\n  2. `research-code-simplifier` resolved at `.codex/agents/research-code-simplifier.toml`.\n  3. The launched delegate reported that it resolved and invoked `research-repo-standard:research-repo-standard` from `[source@commit]`, with no implementation requested.\n\n  If any of these checks could not run, host integration is not complete. I am reporting it as a manual boundary and have not claimed it passed.\n\nShould I commit `.codex/agents/research-code-simplifier.toml` with the scaffold now, or leave it uncommitted for your review?"
    }
  ],
  "host_checks": {
    "claude_code": "Claude Code 2.1.283 in a throwaway config: claude plugin validate passed with one warning (no version, intended); marketplace add from the working tree, install, and plugin details listed one skill and one agent with the commit as version. Namespaced names come from the plugin docs; no session smoke test ran.",
    "codex": "codex-cli 0.157.1 in a throwaway CODEX_HOME: marketplace add read .claude-plugin/marketplace.json, plugin add installed under plugins/cache/research-repo-standard/research-repo-standard/local/, and debug prompt-input listed research-repo-standard:research-repo-standard. The derived agent TOML parsed; Codex agent resolution and the session smoke test did not run.",
    "github_install": "After commit 872f117 was pushed, both CLIs added the marketplace from github.com/lucascamillomd/research-repo-standard at branch claude/prompt-audit-checkup-l4dwrr and installed the plugin in throwaway configs. Claude Code reported version 872f11734cda with one skill and one agent; Codex installed under the local version, upgraded the marketplace, and listed research-repo-standard:research-repo-standard."
  },
  "limitations": "One sample per scenario and snapshot. AB passed on the baseline, so it shows no measured improvement. Scenarios C and R, which bootstrap without a selected host, were not rerun. No model-backed host smoke test ran on either host."
}
